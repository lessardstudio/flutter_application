# Nathan 🦊 — калорийный ассистент

Это Flutter‑приложение с бэкендом на FastAPI. Nathan помогает в свободной форме описывать, что вы съели, и считает калории с подсказками.

Ниже — максимально приземлённая инструкция, как:
- поднять проект локально в Docker;
- собрать Flutter Web;
- собрать Android APK;
- подготовить сборку для iOS.

---

## 1. Что нужно установить

Минимальный набор:
- Flutter SDK (актуальная stable‑версия)
- Git
- Docker и Docker Compose

Дополнительно, если хотите собирать под мобильные платформы:
- **Android**: Android Studio + Android SDK + эмулятор или реальное устройство
- **iOS**: macOS, Xcode, установленный Flutter для iOS

Код:
- фронтенд: [lib/main.dart](file:///c:/Users/User/Desktop/Nathan/flutter_application_1/lib/main.dart)
- бэкенд: [main.py](file:///c:/Users/User/Desktop/Nathan/flutter_application_1/main.py)
- Docker: [Dockerfile.frontend](file:///c:/Users/User/Desktop/Nathan/flutter_application_1/Dockerfile.frontend), [Dockerfile.backend](file:///c:/Users/User/Desktop/Nathan/flutter_application_1/Dockerfile.backend), [docker-compose.yaml](file:///c:/Users/User/Desktop/Nathan/flutter_application_1/docker-compose.yaml)

---

## 2. Настройка переменных окружения

Файл [.env](file:///c:/Users/User/Desktop/Nathan/flutter_application_1/.env) **не лежит в Git**, его нужно создать/заполнить локально.

Минимум:

```env
OPENAI_API_KEY=ваш_ключ_OpenAI

# Опционально для OpenRouter (fallback, если OpenAI недоступен)
OPENROUTER_API_KEY=ваш_ключ_OpenRouter
OPENROUTER_MODEL=openai/gpt-4o-mini
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
```

Бэкенд сначала обращается к OpenAI, а если там ошибка или нет ключа — пробует OpenRouter.

---

## 3. Запуск проекта в Docker

В корне проекта уже есть готовый `docker-compose.yaml`. Он поднимает два сервиса:
- `backend` — FastAPI на порту `8000`
- `frontend` — Flutter Web в nginx на порту `8080`

### 3.1. Собрать и запустить

В корне проекта:

```bash
docker compose up -d --build
```

Это:
- соберёт образы фронта и бэка;
- поднимет контейнеры в фоне.

### 3.2. Проверить

- Бэкенд: http://localhost:8000/docs — OpenAPI‑документация FastAPI
- Фронтенд: http://localhost:8080 — приложение Nathan в браузере

Остановить контейнеры:

```bash
docker compose down
```

---

## 4. Сборка Flutter Web вручную

Если Docker не нужен, можно собрать веб‑версию руками.

### 4.1. Установить зависимости

```bash
flutter pub get
```

### 4.2. Собрать Web

```bash
flutter build web --release
```

Готовые статические файлы будут лежать в:

```text
build/web/
```

Эту папку можно отдать любым веб‑сервером (nginx, Apache, любой хостинг статики и т.п.).

---

## 5. Сборка Android APK

Подходит для локального теста и установки на устройства.

### 5.1. Требования

- Установлен Android SDK (обычно через Android Studio)
- Подключён телефон с включённой USB‑отладкой **или** настроен эмулятор

### 5.2. Команда сборки

В корне проекта:

```bash
flutter build apk --release
```

Готовый файл:

```text
build/app/outputs/flutter-apk/app-release.apk
```

Можно:
- скопировать APK на устройство и установить вручную;
- или выполнить:

```bash
flutter install
```

(если устройство подключено и доступно для Flutter).

> Примечание: в [android/app/build.gradle.kts](file:///c:/Users/User/Desktop/Nathan/flutter_application_1/android/app/build.gradle.kts) релиз пока подписывается debug‑ключом — этого достаточно для установки на телефон, но не для публикации в Google Play. Для магазина нужно настроить собственный signing config.

---

## 6. Подготовка iOS‑сборки

Сборка iOS возможна только на macOS.

### 6.1. Требования

- macOS
- Xcode (последняя стабильная версия)
- Установленный Flutter (с поддержкой iOS)

### 6.2. Сборка

В корне проекта:

```bash
flutter pub get
flutter build ios --release
```

Для запуска на устройстве или выгрузки в TestFlight:

1. Открой в Xcode проект из папки [ios](file:///c:/Users/User/Desktop/Nathan/flutter_application_1/ios):
   - файл `Runner.xcworkspace` или `Runner.xcodeproj` (в зависимости от версии Flutter)
2. Выбери:
   - свою команду разработки (Team)
   - целевое устройство (симулятор или реальный iPhone)
3. Собери и задеплой через Xcode.

Настройка сертификатов, профилей и публикации в App Store — стандартная для любого Flutter/iOS‑проекта.

---

## 7. Быстрый чек‑лист для нового разработчика

1. Клонировать репозиторий:
   ```bash
   git clone https://github.com/lessardstudio/flutter_application.git
   cd flutter_application_1
   ```
2. Создать `.env` по примеру из раздела 2 и прописать ключи.
3. Установить зависимости фронта:
   ```bash
   flutter pub get
   ```
4. Запустить в Docker:
   ```bash
   docker compose up -d --build
   ```
5. Открыть http://localhost:8080 и поиграться с чатом Nathan 🦊.
